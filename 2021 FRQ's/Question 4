pulic static boolean isNonZeroRow(int [][] array2D,  int  r)
{
    for(int i=0; i<array2D[0].size(); i++)
    {
        if(array2D[r][i] == 0)
        {
            return false;
        }
    }
    return true;
}

public static int [][] resize(int[][] array2D)
{
    for(int i=0; i<array2D.size(); i++)
    {
        for(int j=0; j<array2D[0].size(); j++)
        {
            if(isNonZeroRow(array2D, i))
        }
    }
}
